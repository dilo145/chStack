import {Level} from "@/types/Level";
import {Category} from "@/types/Category";

export type Former = {
  speciality: string;
  firstName: string,
  lastName: string,
  email: string,
  password: string,
  photo: string
};
